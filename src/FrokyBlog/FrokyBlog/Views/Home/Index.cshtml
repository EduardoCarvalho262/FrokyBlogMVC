@model IEnumerable<FrokyBlog.Models.Post>

@{
    ViewData["Title"] = "Home Page";
}

<form asp-controller="Home" asp-action="Index" method="get">
    <p>
        Title: <input type="text" name="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>

<div class="text-end mb-3">
    <a class="btn btn-outline-primary" asp-controller="Posts" asp-action="Create">New Post</a>
</div>


@foreach (var item in Model)
{
    <div class="text-center mb-4">
        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title">@Html.DisplayFor(modelItem => item.Title)</h5>
                <p class="card-text">@Html.DisplayFor(modelItem => item.Description)</p>
                <a class="btn btn-outline-primary" asp-controller="Posts" asp-action="Details" asp-route-id="@item.Id">Ver Post</a>
            </div>
        </div>
    </div>
}



